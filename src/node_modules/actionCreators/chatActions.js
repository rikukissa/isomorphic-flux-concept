'use strict';
var appDispatcher = require('dispatcher');

module.exports = {
  sendMessage(message) {
    appDispatcher.handleUserAction({
      type: 'SEND_CHAT_MESSAGE',
      data: message
    });
  }
};
